[pytest]
markers =
    core: Tests for basic UI elements and core application features (non-auth).
    auth: Tests for the standard OIDC authentication and token handling flows.
    security: Tests for security, protocol correctness, and error handling in the OIDC flow.
    performance: Tests for performance metrics (response time, page load, etc.).